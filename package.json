{
  "name": "quest-mentoring-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "lint": "pnpm -r lint",
    "build": "pnpm -r build",
    "dev": "pnpm --filter frontend dev"
  },
  "dependencies": {
    "clsx": "^2.1.1",
    "react-icons": "^5.5.0",
    "swiper": "^11.2.10"
  }
}
